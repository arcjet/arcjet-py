[project]
name = "example-flask"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["arcjet", "flask>=3.1.2"]

[tool.uv.sources]
arcjet = { path = "../../", editable = true }
