[project]
name = "example-fastapi-guard-policy"
version = "0.1.0"
description = "Arcjet Python SDK remote Guard policy example"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "arcjet[sensitive-info-rampart]",
    "fastapi[standard]>=0.124.4",
    "langchain>=1.2.10",
    "langchain-openai>=1.1.10",
]

[tool.uv]
exclude-newer = "7 days"

[tool.uv.sources]
arcjet = { path = "../../", editable = true }
arcjet-sensitive-info-rampart = { path = "../../sensitive-info-rampart", editable = true }
