# Mocked tests

Tests in this directory (and subdirectories) use mocked protobuf etc. See
[`./conftest.py`](./conftest.py) for details.

To write tests without mocking, put them outside this directory.